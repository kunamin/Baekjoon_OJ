#include <bits/stdc++.h>

using namespace std;

int main()
{
    vector<int> vec;
    int N;
    int temp;
    cin>> N;

    for(int i=0; i<N; i++){
        cin>>temp;
        vec.push_back(temp);

    }

    vector<int> vec1(N);
    vector<int> vec2;
    vector<int> vec3;
    int temp1=0;
    int temp2=0;
    for(int i=0; i<N-1; i++){
        if(vec[i]>vec[i+1]){
            if(temp =0)
                temp2=i;
        }
    }

    while(true){
        vec1=vec;
        for(int i=0; i<N; i++){
            for (int)
        }
    }
    return 0;
}
