#include <iostream>

using namespace std;

int main()
{
    int N;
    cin >> N;
    vector< vector<int> > vec1;
    vector<int> vec2(4);
    vector< vector<int> > mon;
    vector<int> day;

    for (int i=0; i<N; i++){
        for(int j=0; j<4; j++){
            cin >> vec2[j];
        }

        vec1.push_back();
    }
    return 0;
}
