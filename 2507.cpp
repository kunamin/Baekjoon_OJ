#include <bits/stdc++.h>

using namespace std;

int main()
{
    int N;
    cin >> N;
    vector< vector<int> > vec1;
    vector<int> vec2;
    int temp;
    for(int i=0; i<N; i++){
        for(int j=0; j<3; j++){
            cin >> temp;
            vec2.push_back(temp);
        }
        vec1.push_back(vec2);
    }

    vector< vector<int> > tempv;
    int x=0;
    int y=0;
    while(true){

    }
    return 0;
}
